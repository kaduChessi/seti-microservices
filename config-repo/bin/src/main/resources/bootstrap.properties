spring.application.name = ban-config
spring.profiles.active = native
eureka.instance.nonSecurePort = ${server.port:8888}
eureka.client.serviceUrl.defaultZone = http://${eureka.host:localhost}:${eureka.port:8761}/eureka/